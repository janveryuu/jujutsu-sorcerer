# Agent Rules

- **ALWAYS PUSH TO GITHUB/VERCEL:** When you make any code changes to this project, you must ALWAYS commit and push them to GitHub (`git add`, `git commit`, `git push`) so that Vercel can deploy the changes. The user tests the live Vercel deployment, so they will not see your changes unless they are pushed.
